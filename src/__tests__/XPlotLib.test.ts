import { XPlotLib } from '../index';
test('XPlotLib', () => {
  // indexedDB is not available in Node, but should be available in the browser...
  //   const lib = new XPlotLib();
  //   lib.add({ timestamp: Date.now(), value: 123 });
});
